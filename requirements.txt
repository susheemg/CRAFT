# Pinned to the versions this build was tested against. Upgrades belong in a
# change with a test run behind them, not in a deploy.

# Web
fastapi==0.141.1
starlette==1.6.0
uvicorn[standard]==0.52.3
jinja2==3.1.6
python-multipart==0.0.32

# Data
SQLAlchemy==2.0.52
psycopg[binary]==3.3.4

# Configuration
pydantic==2.13.4
pydantic-settings==2.15.0
python-dotenv==1.2.2

# Security
python-jose[cryptography]==3.5.0
bcrypt==5.0.0
cryptography==46.0.6

# Outbound: LLM providers, webhooks, MCP clients
httpx==0.28.1
